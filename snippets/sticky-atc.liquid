{% if product and product.handle == 'bakuchiol-age-delay-serum' %}
  {% assign sticky_variant = product.selected_or_first_available_variant %}

  {% if sticky_variant.available %}
    {% assign compare_price = sticky_variant.compare_at_price %}
    {% assign current_price = sticky_variant.price %}

    {% if compare_price > current_price %}
      {% assign savings = compare_price | minus: current_price %}
      {% assign savings_pct = savings | times: 100 | divided_by: compare_price | floor %}
    {% endif %}

    <div id="bakuchiol-sticky-atc" class="sticky-atc" aria-hidden="true">
      <div class="page-width sticky-atc__outer">
        <div class="sticky-atc__inner">
          <div class="sticky-atc__left">
            <div class="sticky-atc__thumb">
              {{ product.featured_image | image_url: width: 240 | image_tag: loading: 'lazy', alt: product.title }}
            </div>

            <div class="sticky-atc__info">
              <!-- Rating row -->
              <div class="sticky-atc__rating">
                <span class="sticky-atc__stars">★★★★★</span>
                <span class="sticky-atc__rating-text">
                  Excellent <strong>4.8</strong> | 482+ reviews
                </span>
              </div>

              <p class="sticky-atc__title">
                {{ product.title }}
              </p>

              <div class="sticky-atc__meta">
                <span class="sticky-atc__price">
                  {{ current_price | money }}
                </span>

                {% if compare_price > current_price %}
                  <span class="sticky-atc__price--compare">
                    {{ compare_price | money }}
                  </span>

                  {% if savings_pct > 0 %}
                    <span class="sticky-atc__badge">
                      SAVE {{ savings_pct }}%
                    </span>
                  {% endif %}
                {% endif %}
              </div>
            </div>
          </div>

          <button
            type="button"
            class="sticky-atc__button js-sticky-atc-add baku-gradient"
          >
            ADD TO CART
          </button>
        </div>
      </div>
    </div>
  {% endif %}
{% endif %}

<style>
/* ================================
   MAIN LUXURY GRADIENT BUTTON
   (used on product form, etc.)
================================ */

.baku-gradient-button {


  background-size: cover;
  background-repeat: no-repeat;
  background-clip: padding-box;

  width: 100%;
  padding: 18px 22px;

  border: none !important;
  outline: none !important;

  color: #ffffff !important;
  font-weight: 700;
  font-size: 18px;
  text-transform: uppercase;
  letter-spacing: 1px;
  text-align: center;
  cursor: pointer;

  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;

  transition: transform 0.15s ease, opacity 0.2s ease;
}

.baku-gradient-button::after {
  content: none !important;
}

.baku-gradient-button:hover {
  opacity: 0.95;
  transform: translateY(-2px);
}

.baku-gradient-button:active {
  opacity: 0.85;
  transform: translateY(0);
}

.baku-gradient-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.baku-gradient-button__text {
  position: relative;
  z-index: 2;
}

/* ================================
   STICKY ATC BAR
================================ */

/* Visibility container */

.sticky-atc {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 999;
  transform: translateY(100%);
  transition: transform 0.25s ease-out;
  pointer-events: none;
}

.sticky-atc--visible {
  transform: translateY(0);
}

.sticky-atc__outer {
  padding: 1rem 1.5rem;
  background: linear-gradient(to top, #f3f1ee, #ffffff);
  border-top: 1px solid #e2ddd7;
}

.sticky-atc__inner {
  pointer-events: auto;
  display: flex;
  flex-direction: row;      /* force horizontal, even on mobile */
  align-items: center;
  justify-content: space-between;
  gap: 2.25rem;
  padding: 1rem 0;
  flex-wrap: nowrap;
}

/* Left side (image + info) */

.sticky-atc__left {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 1.25rem;
  min-width: 0;
  flex: 1 1 auto;
}

.sticky-atc__thumb {
  width: 82px;
  height: 82px;
  background: #f5f2ee;
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
  flex-shrink: 0;
}

.sticky-atc__thumb img {
  width: 100%;
  height: auto;
}

/* Text content */

.sticky-atc__info {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
  min-width: 0;
}

/* Ratings */

.sticky-atc__rating {
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

.sticky-atc__stars {
  color: #b79cff;
  font-size: 1.45rem;
  letter-spacing: 0.12em;
}

.sticky-atc__rating-text {
  color: #252347;
  font-size: 1.05rem;
}

.sticky-atc__rating-text strong {
  font-weight: 700;
}

/* Title */

.sticky-atc__title {
  margin: 0;
  font-size: 1.35rem;
  font-weight: 700;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Price row */

.sticky-atc__meta {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.sticky-atc__price {
  font-size: 1.25rem;
  font-weight: 700;
}

.sticky-atc__price--compare {
  font-size: 1.1rem;
  opacity: 0.6;
  text-decoration: line-through;
}

.sticky-atc__badge {
  background-color: #5b4836;
  color: #fff;
  font-size: 0.9rem;
  padding: 0.2rem 0.5rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

/* Sticky ATC button (gradient, right side) */

.sticky-atc__button {
  --baku-start: #1a152f;
  --baku-mid:   #5a3b8c;
  --baku-end:   #8d6fd1;

  background: linear-gradient(90deg, var(--baku-start), var(--baku-mid), var(--baku-end));
  background-size: cover;
  background-repeat: no-repeat;
  background-clip: padding-box;

  border: none !important;
  outline: none !important;
  border-radius: 0;

  color: #ffffff !important;
  font-weight: 800;
  font-size: 1.4rem;
  text-transform: uppercase;
  letter-spacing: 0.16em;
  text-align: center;
  cursor: pointer;

  padding: 1.4rem 3.8rem;
  margin-right: 0.5rem;

  white-space: nowrap;
  box-shadow: none;

  flex-shrink: 0;          /* keep button width on mobile */
  transition: transform 0.15s ease, opacity 0.2s ease;
}

.sticky-atc__button:hover {
  opacity: 0.95;
  transform: translateY(-2px);
}

.sticky-atc__button:active {
  opacity: 0.8;
  transform: translateY(0);
}

.sticky-atc__button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* ===== Responsive tweaks ===== */

@media (max-width: 900px) {
  .sticky-atc__outer {
    padding: 0.75rem 0.9rem;
  }

  .sticky-atc__thumb {
    width: 70px;
    height: 70px;
  }

  .sticky-atc__title {
    font-size: 1.2rem;
  }

  .sticky-atc__stars {
    font-size: 1.2rem;
  }

  .sticky-atc__rating-text {
    font-size: 0.95rem;
  }

  .sticky-atc__price {
    font-size: 1.15rem;
  }

  .sticky-atc__button {
    padding: 1.1rem 2.4rem;
    font-size: 1.2rem;
    letter-spacing: 0.14em;
  }
}

@media (max-width: 600px) {
  .sticky-atc__inner {
    gap: 1.25rem;
  }

  .sticky-atc__title {
    max-width: 11rem;
  }

  .sticky-atc__rating-text {
    display: none; /* keeps bar cleaner on very small screens */
  }
}
</style>
<script>
document.addEventListener('DOMContentLoaded', function () {
  var bar = document.getElementById('bakuchiol-sticky-atc');
  if (!bar) return;

  var mainAtc =
    document.querySelector('form[action^="/cart/add"] .product-form__submit') ||
    document.querySelector('.product-form__submit');

  if (!mainAtc) return;

  var button = bar.querySelector('.js-sticky-atc-add');

  function toggleBar() {
    var rect = mainAtc.getBoundingClientRect();
    var outOfView = rect.bottom < 0 || rect.top > window.innerHeight;
    var scrolledEnough = window.scrollY > 200;

    /* --- FOOTER CHECK --- */
    var scrollBottom = window.innerHeight + window.scrollY;
    var pageHeight = document.body.offsetHeight;
    var nearFooter = scrollBottom >= pageHeight - 250; // hide 250px before bottom

    if (outOfView && scrolledEnough && !nearFooter) {
      bar.classList.add('sticky-atc--visible');
      bar.setAttribute('aria-hidden', 'false');
    } else {
      bar.classList.remove('sticky-atc--visible');
      bar.setAttribute('aria-hidden', 'true');
    }
  }

  window.addEventListener('scroll', toggleBar);
  window.addEventListener('resize', toggleBar);
  toggleBar();

  button.addEventListener('click', function () {
    if (mainAtc) mainAtc.click();
  });
});
</script>