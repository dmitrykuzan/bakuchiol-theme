<section class="baku-hero">
  <div class="baku-hero__inner page-width">
    
    <!-- LEFT: TEXT -->
    <div class="baku-hero__copy">
      <p class="baku-hero__eyebrow">
        1.0% BAKUCHIOL + PEPTIDES
      </p>

      <h1 class="baku-hero__title">
        Glass-skin results.<br>
        Retinol power.<br>
        <span class="accent">Zero irritation.</span>
      </h1>

      <p class="baku-hero__subtitle">
        Bakuchiol Age Delay Serum visibly softens fine lines, boosts radiance,
        and keeps even sensitive skin calm and comfortable.
      </p>

      <a href="/products/bakuchiol-age-delay-serum"
         class="baku-hero__button">
        SHOP NOW
      </a>

      <p class="baku-hero__social-proof">
        ★★★★★ Loved by sensitive skin &amp; first-time retinol users.
      </p>
    </div>

    <!-- RIGHT: HERO IMAGE -->
    <div class="baku-hero__visual">
      <picture>
        <source srcset="{{ 'bakuchiol-hero-model.png' | asset_url }}" media="(max-width: 749px)">
        <img
          src="{{ 'bakuchiol-hero-model.png' | asset_url }}"
          alt="Woman holding Bakuchiol Age Delay Serum bottle"
          loading="lazy"
          class="baku-hero__img">
      </picture>
    </div>

  </div>
</section>

<style>
  /* ===========================================
   GLOBAL COLOR VARIABLES
=========================================== */
:root {
  --ink: #151323;
  --ink-soft: rgba(21, 19, 35, 0.7);
  --lavender-accent: #b19cd6;    /* <<< your purple */
  --hero-bg: linear-gradient(135deg, #f5f1ff 0%, #ffffff 40%, #f6f2ff 100%);
}

/* ===========================================
   HERO WRAPPER
=========================================== */

.baku-hero {
  background: var(--hero-bg);
  color: var(--ink);
}

.baku-hero__inner {
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.1fr);
  align-items: center;
  min-height: clamp(520px, 80vh, 760px);
  padding-block: 4rem;
  gap: 3.5rem;
}

/* ===========================================
   ACCENT PURPLE TEXT
=========================================== */

.accent {
  color: var(--lavender-accent);
}

/* ===========================================
   LEFT COLUMN CONTENT
=========================================== */

.baku-hero__copy {
  max-width: 540px;
}

.baku-hero__eyebrow {
  margin: 0 0 1.2rem;
  font-size: 0.85rem;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: var(--ink-soft);
}

.baku-hero__title {
  margin: 0 0 1.8rem;
  font-size: clamp(2.4rem, 2.7vw + 1.7rem, 3.3rem);
  line-height: 1.1;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.baku-hero__subtitle {
  margin: 0 0 2.4rem;
  font-size: 1rem;
  line-height: 1.8;
  color: var(--ink-soft);
  max-width: 480px;
}

/* ===========================================
   CTA BUTTON (SHARP EDGES)
=========================================== */

.baku-hero__button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 1rem 3.4rem;
  border-radius: 0; /* SHARP */
  font-size: 0.9rem;
  font-weight: 600;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  background-color: var(--ink);
  color: #ffffff;
  border: 1px solid var(--ink);
  cursor: pointer;
  text-decoration: none;
  transition: all 140ms ease;
}

.baku-hero__button:hover {
  background-color: #ffffff;
  color: var(--ink);
  border-color: var(--ink);
  transform: translateY(-1px);
  box-shadow: 0 10px 28px rgba(13, 9, 34, 0.16);
}

/* ===========================================
   SOCIAL PROOF
=========================================== */

.baku-hero__social-proof {
  margin: 1.8rem 0 0;
  font-size: 0.78rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--ink-soft);
}

/* ===========================================
   RIGHT IMAGE
=========================================== */

.baku-hero__visual {
  display: flex;
  justify-content: flex-end;
  align-items: flex-end;
}

.baku-hero__img {
  height: min(88vh, 720px);
  width: auto;
  max-width: 100%;
  object-fit: contain;
  transform: translateX(6%);
}

@media (min-width: 1400px) {
  .baku-hero__inner {
    grid-template-columns: minmax(0, 1.05fr) minmax(0, 1.25fr);
  }
}

/* ===========================================
   RESPONSIVE BEHAVIOR
=========================================== */

@media (max-width: 989px) {
  .baku-hero__inner {
    grid-template-columns: 1fr;
    gap: 2.6rem;
    padding-block: 3.4rem;
  }

  .baku-hero__img {
    width: min(420px, 80vw);
    height: auto;
    transform: none;
  }
}

@media (max-width: 640px) {
  .baku-hero {
    background: #ffffff;
  }

  .baku-hero__button {
    width: 100%;
  }
}

/* ===========================================
   DESKTOP – MAKE TEXT LARGER + MORE DOMINANT
=========================================== */
@media (min-width: 990px) {

  /* Make left side take more width */
  .baku-hero__inner {
    grid-template-columns: 1.25fr 1fr !important;
    gap: 4rem !important;
  }

  /* Larger title */
  .baku-hero__title {
    font-size: clamp(3rem, 3.4vw + 1.2rem, 4rem) !important;
    line-height: 1.08 !important;
  }

  /* Larger subtitle */
  .baku-hero__subtitle {
    font-size: 1.15rem !important;
    line-height: 1.75 !important;
    max-width: 500px !important;
  }

  /* Button slightly bigger */
  .baku-hero__button {
    padding: 1.2rem 3.8rem !important;
    font-size: 1rem !important;
    letter-spacing: 0.2em !important;
  }

  /* Social proof a bit larger too */
  .baku-hero__social-proof {
    font-size: 0.9rem !important;
  }

  /* Adjust image scale so text feels stronger */
  .baku-hero__img {
    height: min(80vh, 670px) !important;
    transform: translateX(2%) !important;
  }
}
</style>