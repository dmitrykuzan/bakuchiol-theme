<section class="bk-hero">
  <div class="bk-hero__inner page-width">
    <!-- LEFT: COPY -->
    <div class="bk-hero__copy">
      <p class="bk-hero__eyebrow">
        1.0% BAKUCHIOL + PEPTIDES
      </p>

      <h1 class="bk-hero__headline">
        Glass-skin results.<br>
        Retinol power.<br>
        Zero irritation.
      </h1>

      <p class="bk-hero__subcopy">
        Bakuchiol Age Delay Serum visibly softens fine lines, boosts radiance,
        and keeps even sensitive skin calm and comfortable.
      </p>

      <div class="bk-hero__actions">
        <a href="/products/bakuchiol-age-delay-serum" class="bk-hero__btn">
          Shop now
        </a>

        <p class="bk-hero__note">
          ★★★★★ Loved by sensitive skin &amp; first-time retinol users.
        </p>
      </div>
    </div>

    <!-- RIGHT: MODEL IMAGE -->
    <div class="bk-hero__media">
      <picture>
        <!-- Mobile crop -->
        <source
          media="(max-width: 749px)"
          srcset="{{ 'bakuchiol-hero-model-mobile.jpg' | asset_url }}"
        >
        <!-- Desktop hero -->
        <img
          src="{{ 'bakuchiol-hero-model-desktop.jpg' | asset_url }}"
          alt="Woman holding Bakuchiol Age Delay Serum against a clean white background"
          class="bk-hero__img"
          loading="eager"
          fetchpriority="high"
        >
      </picture>
    </div>
  </div>
</section>

<style>
  /* ===========================
   BAKUCHIOL HERO
   =========================== */

:root {
  --bk-ink: #1a152f;
  --bk-ink-soft: #48445f;
  --bk-bg-soft: #faf7ff;
  --bk-btn: #1a152f;
}

/* SECTION BACKGROUND */
.bk-hero {
  position: relative;
  overflow: hidden;
  background: radial-gradient(circle at top left, #f4edff 0, #ffffff 45%, #f8f3ff 100%);
}

/* INNER LAYOUT */
.bk-hero__inner {
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
  align-items: center;
  gap: 4rem;
  padding: 7rem 1.5rem 6rem;
}

/* COPY COLUMN */
.bk-hero__copy {
  max-width: 520px;
  color: var(--bk-ink);
}

.bk-hero__eyebrow {
  margin: 0 0 1rem;
  font-size: 0.78rem;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: var(--bk-ink-soft);
}

.bk-hero__headline {
  margin: 0 0 1.5rem;
  font-size: clamp(2.4rem, 3.3vw + 1rem, 3.6rem);
  line-height: 1.06;
  letter-spacing: 0.02em;
  font-weight: 600;
}

.bk-hero__subcopy {
  margin: 0 0 2.2rem;
  font-size: 1rem;
  line-height: 1.7;
  color: var(--bk-ink-soft);
  max-width: 460px;
}

/* CTA */
.bk-hero__actions {
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
}

.bk-hero__btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 1rem 3.1rem;
  border-radius: 999px;
  border: none;
  text-decoration: none;
  text-transform: uppercase;
  letter-spacing: 0.16em;
  font-size: 0.9rem;
  font-weight: 600;
  background-color: var(--bk-btn);
  color: #ffffff;
  box-shadow: 0 16px 40px rgba(26, 21, 47, 0.18);
  transition: transform 150ms ease, box-shadow 150ms ease,
    background-color 150ms ease;
}

.bk-hero__btn:hover {
  background-color: #221b44;
  transform: translateY(-1px);
  box-shadow: 0 22px 60px rgba(26, 21, 47, 0.26);
}

.bk-hero__note {
  margin: 0;
  font-size: 0.78rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--bk-ink-soft);
}

/* IMAGE COLUMN */
.bk-hero__media {
  position: relative;
  justify-self: flex-end;
  max-width: 540px;
}

.bk-hero__img {
  display: block;
  width: 100%;
  height: auto;
  object-fit: cover;
}

/* Optional soft vignette behind image */
.bk-hero__media::after {
  content: "";
  position: absolute;
  inset: 12% -8% -18%;
  border-radius: 40% 40% 45% 45%;
  background: radial-gradient(circle at 20% 0, #ffffff 0, #ede6ff 50%, #e0d5ff 100%);
  z-index: -1;
}

/* ===========================
   RESPONSIVE
   =========================== */

@media (max-width: 1024px) {
  .bk-hero__inner {
    grid-template-columns: 1fr;
    padding: 4.5rem 1.5rem 4.5rem;
    row-gap: 3rem;
  }

  .bk-hero__copy {
    max-width: 100%;
  }

  .bk-hero__media {
    justify-self: center;
    max-width: 420px;
  }

  .bk-hero__media::after {
    inset: 8% -4% -10%;
  }
}

@media (max-width: 749px) {
  .bk-hero__inner {
    padding: 4rem 1.25rem 3.5rem;
  }

  .bk-hero__eyebrow {
    font-size: 0.7rem;
  }

  .bk-hero__headline {
    font-size: 2.2rem;
  }

  .bk-hero__subcopy {
    font-size: 0.95rem;
  }

  .bk-hero__actions {
    align-items: flex-start;
  }

  .bk-hero__media {
    max-width: 100%;
  }
}
</style>