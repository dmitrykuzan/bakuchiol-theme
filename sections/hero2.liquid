<section class="baku-hero">
  <div class="baku-hero__inner page-width">
    
    <!-- LEFT: TEXT -->
    <div class="baku-hero__copy">
      <p class="baku-hero__eyebrow">
        1.0% BAKUCHIOL + PEPTIDES
      </p>

      <h1 class="baku-hero__title">
        Glass-skin results.<br>
        Retinol power.<br>
        <span class="accent">Zero irritation.</span>
      </h1>

      <p class="baku-hero__subtitle">
        Bakuchiol Age Delay Serum visibly softens fine lines, boosts radiance,
        and keeps even sensitive skin calm and comfortable.
      </p>

      <a href="/products/bakuchiol-age-delay-serum"
         class="baku-hero__button">
        SHOP NOW
      </a>

      <p class="baku-hero__social-proof">
        ★★★★★ Loved by sensitive skin &amp; first-time retinol users.
      </p>
    </div>

    <!-- RIGHT: HERO IMAGE -->
    <div class="baku-hero__visual">
      <picture>
        <source srcset="{{ 'bakuchiol-hero-model.png' | asset_url }}" media="(max-width: 749px)">
        <img
          src="{{ 'bakuchiol-hero-model.png' | asset_url }}"
          alt="Woman holding Bakuchiol Age Delay Serum bottle"
          loading="lazy"
          class="baku-hero__img">
      </picture>
    </div>

  </div>
</section>

<style>
  /* ===========================================
   GLOBAL COLOR VARIABLES
=========================================== */
:root {
  --ink: #151323;
  --ink-soft: rgba(21, 19, 35, 0.7);
  --lavender-accent: #b19cd6; 
  --hero-bg: linear-gradient(135deg, #f5f1ff 0%, #ffffff 40%, #f6f2ff 100%);
}

/* ===========================================
   HERO WRAPPER
=========================================== */

.baku-hero {
  color: var(--ink);
}

.baku-hero__inner {
  display: grid;
  grid-template-columns: minmax(0, 1.3fr) minmax(0, 1fr);
  align-items: center;
  min-height: clamp(600px, 85vh, 820px);
  gap: 4.2rem;
}

/* ===========================================
   ACCENT PURPLE
=========================================== */
.accent {
  color: var(--lavender-accent);
}

/* ===========================================
   LEFT COLUMN CONTENT
=========================================== */

.baku-hero__copy {
  max-width: 600px;
}

/* Eyebrow text */
.baku-hero__eyebrow {
  margin: 0 0 1.6rem;
  font-size: 1rem;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  color: var(--ink-soft);
}

/* MAIN TITLE — BIGGER */
.baku-hero__title {
  margin: 0 0 2.4rem;
  font-size: clamp(3.2rem, 3.8vw + 1.8rem, 5rem);
  line-height: 1.04;
  letter-spacing: 0.065em;
  text-transform: uppercase;
  font-weight: 600;
}

/* SUBTITLE — BIGGER + MORE READABLE */
.baku-hero__subtitle {
  margin: 0 0 3rem;
  font-size: 1.35rem;
  line-height: 1.75;
  color: var(--ink-soft);
  max-width: 540px;
}

/* ===========================================
   CTA BUTTON — SHARP
=========================================== */

.baku-hero__button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 1.4rem 4.2rem;
  border-radius: 0;
  font-size: 1.05rem;
  font-weight: 700;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  background-color: var(--ink);
  color: #ffffff;
  border: 1px solid var(--ink);
  cursor: pointer;
  text-decoration: none;
  transition: all 140ms ease;
}

.baku-hero__button:hover {
  background-color: #ffffff;
  color: var(--ink);
  border-color: var(--ink);
  transform: translateY(-2px);
  box-shadow: 0 18px 34px rgba(13, 9, 34, 0.18);
}

/* ===========================================
   SOCIAL PROOF — BIGGER + CLEANER
=========================================== */

.baku-hero__social-proof {
  margin: 2.2rem 0 0;
  font-size: 1rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--ink-soft);
}

/* ===========================================
   RIGHT IMAGE
=========================================== */

.baku-hero__visual {
  display: flex;
  justify-content: flex-end;
  align-items: flex-end;
}

.baku-hero__img {
  height: min(85vh, 760px);
  width: auto;
  max-width: 100%;
  object-fit: contain;
  transform: translateX(3%);
}

/* Huge screens: make proportions even better */
@media (min-width: 1600px) {
  .baku-hero__inner {
    grid-template-columns: 1.4fr 1fr;
  }

  .baku-hero__title {
    font-size: 5.4rem;
  }

  .baku-hero__subtitle {
    font-size: 1.5rem;
  }
}

/* ===========================================
   TABLET + MOBILE
=========================================== */

@media (max-width: 989px) {
  .baku-hero__inner {
    grid-template-columns: 1fr;
    gap: 3rem;
    padding-block: 3.5rem;
    text-align: left;
  }

  .baku-hero__title {
    font-size: 3rem;
  }

  .baku-hero__subtitle {
    font-size: 1.2rem;
    max-width: 100%;
  }

  .baku-hero__img {
    width: min(430px, 90vw);
    height: auto;
    transform: none;
  }
}

@media (max-width: 640px) {
  .baku-hero {
  }

  .baku-hero__button {
    width: 100%;
  }
}
</style>