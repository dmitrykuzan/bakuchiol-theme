<section class="baku-hero">
  <div class="baku-hero__inner page-width">
    <!-- LEFT: COPY -->
    <div class="baku-hero__copy">
      <p class="baku-hero__eyebrow">
        1.0% BAKUCHIOL + PEPTIDES
      </p>

      <h1 class="baku-hero__title">
        Glass-skin results.<br>
        Retinol power.<br>
        Zero irritation.
      </h1>

      <p class="baku-hero__subtitle">
        Bakuchiol Age Delay Serum visibly softens fine lines, boosts radiance,
        and keeps even sensitive skin calm and comfortable.
      </p>

      <a href="/products/bakuchiol-age-delay-serum"
         class="baku-hero__button button button--primary">
        SHOP NOW
      </a>

      <p class="baku-hero__social-proof">
        ★★★★★ Loved by sensitive skin &amp; first-time retinol users.
      </p>
    </div>

    <!-- RIGHT: IMAGE -->
    <div class="baku-hero__visual">
      <picture>
        <!-- Mobile source -->
        <source
          srcset="{{ 'baku-hero-model-mobile.jpg' | asset_url }}"
          media="(max-width: 749px)">

        <!-- Desktop default -->
        <img
          src="{{ 'baku-hero-model-desktop.jpg' | asset_url }}"
          alt="Woman holding Bakuchiol Age Delay Serum bottle"
          loading="lazy"
          class="baku-hero__img">
      </picture>
    </div>
  </div>
</section>
<style>
/* ============================
   HERO WRAPPER
============================ */

.baku-hero {
  background: radial-gradient(140% 200% at 0% 0%,
      #f6f1ff 0%,
      #ffffff 45%,
      #ffffff 100%);
  color: #151323;
}

.baku-hero__inner {
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.1fr);
  align-items: center;
  min-height: clamp(520px, 80vh, 760px);
  padding-block: 4rem;
  gap: 3rem;
}

/* ============================
   LEFT COLUMN – COPY
============================ */

.baku-hero__copy {
  max-width: 520px;
}

.baku-hero__eyebrow {
  margin: 0 0 1.2rem;
  font-size: 0.9rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: rgba(21, 19, 35, 0.6);
}

.baku-hero__title {
  margin: 0 0 1.6rem;
  font-size: clamp(2.4rem, 2.8vw + 1.6rem, 3.4rem);
  line-height: 1.1;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.baku-hero__subtitle {
  margin: 0 0 2.4rem;
  font-size: 1rem;
  line-height: 1.7;
  color: rgba(21, 19, 35, 0.76);
  max-width: 460px;
}

.baku-hero__button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 1.1rem 3.5rem;
  border-radius: 999px;
  font-size: 0.9rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  text-decoration: none;
  background-color: #151323;
  color: #ffffff;
  border: 0;
  cursor: pointer;
  transition: transform 140ms ease, box-shadow 140ms ease,
    background-color 140ms ease;
}

.baku-hero__button:hover {
  transform: translateY(-1px);
  background-color: #211d40;
  box-shadow: 0 16px 40px rgba(17, 12, 43, 0.22);
}

.baku-hero__social-proof {
  margin: 1.6rem 0 0;
  font-size: 0.8rem;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: rgba(21, 19, 35, 0.62);
}

/* ============================
   RIGHT COLUMN – VISUAL
============================ */

.baku-hero__visual {
  position: relative;
  display: flex;
  justify-content: flex-end;
  align-items: flex-end;
  height: 100%;
}

.baku-hero__img {
  height: min(88vh, 720px);
  width: auto;
  max-width: 100%;
  object-fit: contain;
  display: block;
  transform: translateX(6%);
}

/* Gives that Sungboon “bleed to the edge” feel on huge screens */
@media (min-width: 1400px) {
  .baku-hero__inner {
    grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.2fr);
  }
}

/* ============================
   RESPONSIVE – TABLET & MOBILE
============================ */

@media (max-width: 989px) {
  .baku-hero__inner {
    grid-template-columns: 1fr;
    gap: 2.5rem;
    min-height: auto;
    padding-block: 3.5rem;
  }

  .baku-hero__copy {
    max-width: 100%;
  }

  .baku-hero__title {
    font-size: clamp(2.1rem, 5vw, 2.7rem);
  }

  .baku-hero__visual {
    justify-content: center;
    align-items: flex-end;
  }

  .baku-hero__img {
    height: auto;
    width: min(420px, 80vw);
    transform: translateX(0);
  }
}

@media (max-width: 640px) {
  .baku-hero {
    background: #ffffff;
  }

  .baku-hero__inner {
    padding-block: 2.8rem 3.2rem;
  }

  .baku-hero__copy {
    text-align: left;
  }

  .baku-hero__subtitle {
    max-width: 100%;
  }

  .baku-hero__button {
    width: 100%;
    justify-content: center;
  }

  .baku-hero__social-proof {
    text-align: left;
  }
}
</style>