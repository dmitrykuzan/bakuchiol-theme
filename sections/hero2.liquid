<section class="baku-hero">
  <div class="baku-hero__inner page-width">
    <!-- LEFT: TEXT -->
    <div class="baku-hero__copy">
      <p class="baku-hero__eyebrow">
        1.0% BAKUCHIOL + PEPTIDES
      </p>

      <h1 class="baku-hero__title">
        Glass-skin results.<br>
        Retinol power.<br>
        <span class="accent">Zero irritation.</span>
      </h1>

      <p class="baku-hero__subtitle">
        Bakuchiol Age Delay Serum visibly softens fine lines, boosts radiance,
        and keeps even sensitive skin calm and comfortable.
      </p>

      <a href="/products/bakuchiol-age-delay-serum"
         class="baku-hero__button">
        SHOP NOW
      </a>

      <p class="baku-hero__social-proof">
        ★★★★★ Loved by sensitive skin &amp; first-time retinol users.
      </p>
    </div>

    <!-- RIGHT: HERO IMAGE -->
    <div class="baku-hero__visual">
      <picture>
        <source srcset="{{ 'bakuchiol-hero-model.png' | asset_url }}" media="(max-width: 749px)">
        <img
          src="{{ 'bakuchiol-hero-model.png' | asset_url }}"
          alt="Woman holding Bakuchiol Age Delay Serum bottle"
          loading="lazy"
          class="baku-hero__img">
      </picture>
    </div>
  </div>
</section>

<!-- MARQUEE BANNER – sits directly under hero with no gap -->
<section class="baku-marquee">
  <div class="page-width">
    <div class="baku-marquee__track">
      <div class="baku-marquee__inner">
        <span class="baku-marquee__item">Gentle Retinol Alternative</span>
        <span class="baku-marquee__dot"></span>

        <span class="baku-marquee__item">Peptide-Infused Formula</span>
        <span class="baku-marquee__dot"></span>

        <span class="baku-marquee__item">Supports Firm, Smooth Skin</span>
        <span class="baku-marquee__dot"></span>

        <span class="baku-marquee__item">Free USA Shipping</span>
        <span class="baku-marquee__dot"></span>
      </div>

      <div class="baku-marquee__inner" aria-hidden="true">
        <span class="baku-marquee__item">Gentle Retinol Alternative</span>
        <span class="baku-marquee__dot"></span>

        <span class="baku-marquee__item">Peptide-Infused Formula</span>
        <span class="baku-marquee__dot"></span>

        <span class="baku-marquee__item">Supports Firm, Smooth Skin</span>
        <span class="baku-marquee__dot"></span>

        <span class="baku-marquee__item">Free USA Shipping</span>
        <span class="baku-marquee__dot"></span>
      </div>
    </div>
  </div>
</section>

<!-- SEO text (kept hidden) -->
<section style="display:none !important; visibility:hidden !important; height:0 !important; overflow:hidden !important;">
  <!-- your FAQ content here, unchanged -->
</section>

<style>
<style>
/* ===========================================
   GLOBAL COLOR VARIABLES
=========================================== */
:root {
  --ink: #151323;
  --ink-soft: rgba(21, 19, 35, 0.7);
  --lavender-accent: #b19cd6;
  --hero-bg: linear-gradient(135deg, #f5f1ff 0%, #ffffff 40%, #f6f2ff 100%);
}

/* ===========================================
   HERO WRAPPER
=========================================== */

.baku-hero {
  color: var(--ink);
  margin: 0;                 /* no section spacing from theme */
  padding: 0;                /* let inner handle vertical padding */
  background: var(--hero-bg);
}

.baku-hero__inner {
  display: grid;
  grid-template-columns: minmax(0, 1.3fr) minmax(0, 1fr);
  align-items: center;
  gap: 4.2rem;
  margin: 0;
  padding: 4rem 0 0;         /* top breathing room, NO bottom padding */
  min-height: auto;          /* important: no forced height on desktop */
}

/* ===========================================
   LEFT COLUMN CONTENT
=========================================== */

.baku-hero__copy {
  max-width: 600px;
}

.baku-hero__eyebrow {
  margin: 0 0 1.6rem;
  font-size: 1rem;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  color: var(--ink-soft);
  font-weight: 400;
}

.baku-hero__title {
  margin: 0 0 2.4rem;
  font-size: clamp(3.2rem, 3.8vw + 1.8rem, 5rem);
  line-height: 1.04;
  letter-spacing: 0.065em;
  text-transform: uppercase;
  font-weight: 500;
}

.baku-hero__subtitle {
  margin: 0 0 3rem;
  font-size: 1.35rem;
  line-height: 1.75;
  color: var(--ink-soft);
  max-width: 540px;
  font-weight: 400;
}

/* Accent word */
.accent {
  color: var(--lavender-accent);
}

/* CTA BUTTON — SHARP */

.baku-hero__button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 1.4rem 4.2rem;
  border-radius: 0;
  font-size: 1.05rem;
  font-weight: 500;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  background-color: var(--ink);
  color: #ffffff;
  border: 1px solid var(--ink);
  cursor: pointer;
  text-decoration: none;
  transition: all 140ms ease;
}

.baku-hero__button:hover {
  background-color: #ffffff;
  color: var(--ink);
  border-color: var(--ink);
  transform: translateY(-2px);
  box-shadow: 0 18px 34px rgba(13, 9, 34, 0.18);
}

/* Social proof line */

.baku-hero__social-proof {
  margin: 2.2rem 0 0;
  font-size: 1rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--ink-soft);
}

/* ===========================================
   RIGHT IMAGE — MAKE IT KISS THE MARQUEE
=========================================== */

.baku-hero__visual {
  display: flex;
  justify-content: flex-end;
  align-items: flex-end;
  margin: 0;
  padding: 0;
}

.baku-hero__img {
  display: block;
  height: auto;
  width: auto;
  max-width: 100%;
  object-fit: contain;
  transform: translateX(3%);
  margin: 0;
  padding: 0;
  vertical-align: bottom;      /* removes inline-image extra line-height */
}

/* Kill any theme defaults on images inside hero */
.baku-hero img {
  display: block;
  margin: 0;
  padding: 0;
  border: 0;
}

/* ===========================================
   LARGE DESKTOP TWEAKS
=========================================== */

@media (min-width: 1600px) {
  .baku-hero__inner {
    grid-template-columns: 1.4fr 1fr;
  }

  .baku-hero__title {
    font-size: 5.4rem;
  }

  .baku-hero__subtitle {
    font-size: 1.5rem;
  }
}

/* ===========================================
   TABLET + MOBILE
=========================================== */

@media (max-width: 989px) {
  .baku-hero__inner {
    grid-template-columns: 1fr;
    gap: 3rem;
    text-align: left;
    padding: 3rem 0 2.4rem;    /* still no bottom gap */
  }

  .baku-hero__title {
    font-size: 3rem;
  }

  .baku-hero__subtitle {
    font-size: 1.2rem;
    max-width: 100%;
  }

  .baku-hero__img {
    width: min(430px, 90vw);
    height: auto;
    transform: none;
    margin-inline: auto;
  }

  .baku-hero__button {
    width: 100%;
  }
}

/* ===========================================
   LUXE MARQUEE BAR (directly under hero)
=========================================== */

.baku-marquee {
  background-color: #1a152f;
  color: #ffffff;
  border-top: 1px solid rgba(255,255,255,0.08);
  border-bottom: 1px solid rgba(255,255,255,0.08);
  overflow: hidden;
  font-family: inherit;
  margin: 0;                 /* sits flush to hero & next section */
  padding: 0;
}

.baku-marquee .page-width {
  padding: 12px 0;
}

/* track wrapper */
.baku-marquee__track {
  display: flex;
  white-space: nowrap;
  overflow: hidden;
  position: relative;
}

/* scrolling rows */
.baku-marquee__inner {
  display: inline-flex;
  align-items: center;
  gap: 40px;
  padding-right: 80px;
  animation: baku-marquee-scroll 30s linear infinite;
}

.baku-marquee__track:hover .baku-marquee__inner {
  animation-play-state: paused;
}

/* marquee text */
.baku-marquee__item {
  font-size: 12px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  font-weight: 600;
  opacity: 0.92;
}

/* glowing separator dot */
.baku-marquee__dot {
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: radial-gradient(circle at 30% 30%, #f9f4ff 0%, #b7a4ff 75%);
  box-shadow: 0 0 0 5px rgba(255,255,255,0.05);
  flex-shrink: 0;
}

/* animation */
@keyframes baku-marquee-scroll {
  0% { transform: translateX(0); }
  100% { transform: translateX(-100%); }
}

/* MOBILE marquee tweaks */
@media (max-width: 640px) {
  .baku-marquee .page-width {
    padding: 9px 0;
  }

  .baku-marquee__inner {
    gap: 28px;
    padding-right: 50px;
    animation-duration: 24s;
  }

  .baku-marquee__item {
    font-size: 11px;
    letter-spacing: 0.15em;
  }

  .baku-marquee__dot {
    width: 5px;
    height: 5px;
    box-shadow: 0 0 0 3px rgba(255,255,255,0.05);
  }
}
</style>
</style>